## Setting up the env

1. Switch to branch `memprof`
2. Install `bmlite` with `python=3.12`
3. `cd memory_prof_rep` 
4. `pip install -r extra_requirements.txt`


## Profiling runs

`bash prof.sh`

## Plot

`python plot_prof.py`

